// Create a program that creates two random die(1-6) and compute the sum
// Created Sep 17 2019
public class SumOfTwoDice {
    public static void main(String[] args) {
        int min = 1;
        int max = 6;
        // calculate two numbers from one to 6
        double value1 = Math.floor((Math.random() * ((max - min) + 1)) + min);
        double value2 = Math.floor((Math.random() * ((max - min) + 1)) + min);
        // caluclate the sum and print the total
        double total = value1 + value2;
        System.out.println("The sum is " + total);
    }
}
