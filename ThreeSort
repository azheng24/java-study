// Create a program that takes 3 numbers and sorts them in ascending otder
// Created Sep 17 2019
public class ThreeSort {
    public static void main(String[] args) {
        // make 3 variables that obtain 3 numbers
        int a = Integer.parseInt(args[0]);
        int b = Integer.parseInt(args[1]);
        int c = Integer.parseInt(args[2]);
        // put the first, last, and middle numbers in a varible
        int first = (Math.min(Math.min(a, b), c));
        int last = (Math.max(Math.max(a, b), c));
        int middle = (a + b + c - first - last);
        // print the numbers in ascending order
        System.out.println("The numbers in ascending order is " + first + ", " + middle + ", " + last);
    }
}
